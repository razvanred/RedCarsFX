package carsFX.model;

public class AutoUsata extends Auto {

    private int gg, mm, aaaa;

    public AutoUsata(final Marca marca, final String modello, final Motore motore, final Tipo tipo) {
        super(marca, modello, motore, tipo);
    }
}
